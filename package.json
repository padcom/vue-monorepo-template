{
  "eslintConfig": {
    "extends": [
      "@padcom/eslint-config-vue"
    ]
  },
  "scripts": {
    "start": "turbo dev --no-cache",
    "test": "turbo test",
    "dev": "turbo dev --no-cache",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "git clean -xdf"
  },
  "workspaces": [
    "packages/common",
    "packages/hello",
    "packages/world"
  ],
  "dependencies": {
    "@tailwindcss/typography": "^0.5.9",
    "tailwindcss": "^3.3.3"
  },
  "devDependencies": {
    "turbo": "^1.10.9",
    "typescript": ">= 3.3.0 < 5.1.0",
    "wait-on": "^7.0.1"
  }
}
